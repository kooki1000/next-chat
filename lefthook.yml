pre-commit:
  jobs:
    - name: typecheck
      run: pnpm typecheck

    - name: format & lint
      run: pnpm lint:fix

pre-push:
  jobs:
    - name: typecheck
      run: pnpm typecheck

    - name: format & lint
      run: pnpm lint:fix
